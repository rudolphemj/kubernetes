{{- range .Values.namespaces }}
apiVersion: v1
kind: Service
metadata:
  name: nginx-{{ .name }}
  namespace: {{ .name }}
spec:
  selector:
    app: nginx-{{ .name }}
  ports:
    - port: {{ $.Values.service.port }}
      targetPort: 80
  type: ClusterIP
---
{{- end }}
